{% extends 'base.html.twig' %}

{% block title %}Facturacion | Facturas{% endblock %}

{% block body %}
<nav class="navbar navbar-expand-lg navbar-light" style="background-color: #9aaebd;">
    <span class="navbar-brand mb-0 h1">Facturacion</span>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav ml-auto">
      <a class="nav-link" href="{{path('app_facturacion_dashboard')}}">Facturas</a>
      <a class="nav-link" href="{{path('app_facturacion_tickets')}}">Revisar Tickets</a>
        <a class="nav-link text-light" href="{{path('app_logout')}}"><span class="bi bi-box-arrow-right mr-1"></span>Cerrar sesión</a>
      </div>
    </div>
  </nav>
    <div class="container mt-5 pt-5">
    <div class="row">
      <div class="col-12 col-sm-7 col-md-6 m-auto">
        <div class="card border-0 shadow">
          <div class="card-body">
              <p>Descripcion: {{descripcion}}</p>
              <p>Horas invertidas: {{horasInv}}</p>
              <p>Tipo de trabajo: {{tipoTrabajo}}</p>
              <p>Valor por hora: {{valorHora}}</p>
                {{form_start(form)}}
                <label>Costo total: </label>
                {{form_widget(form.costototal)}}
                <button type="submit" class="btn btn-lg btn-success" >Aceptar</button>
                {{form_end(form)}}
          </div>
        </div>
      </div>
    </div>
  </div>
{% endblock %}
